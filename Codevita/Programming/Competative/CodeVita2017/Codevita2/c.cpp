#include<bits/stdc++.h>
using namespace::std;

#define ll  long long
#define ull unsigned ll
#define LD long double

#define mp make_pair
#define bs binary_search
#define gcd __gcd
#define pb push_back
#define pp pop_back
#define ff first
#define ss second

#define PI acos(-1.0)
#define INF 0x3f3f3f3f
#define INFL 0x3f3f3f3f3f3f3f3fLL

#define rf freopen("input.txt","r",stdin)
#define wf freopen("output.txt","w",stdout)

#define Nitro ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0)
#define clk clock_t tStart = clock()
#define xtime printf("Execution Time : %.5fs\n", (double)(clock() - tStart)/CLOCKS_PER_SEC)

#define sz(a) (int)a.size()
#define pqueue priority_queue
const int MAX = 10005;
ll Arr[MAX+25][MAX+25];

int main()
{
    /*
    int T,N;
    scanf("%d",&T);
    ll Res,x,y,z;
    while(T--)
    {
        pqueue < ll, vector<ll>, greater<ll> > q;
        Res = 0;
        scanf("%d",&N);
        for(int i=0;i<N;i++){
            scanf("%lld",&Arr[i]);
        }

        for(int i=0;i<N;i++){
            q.push(Arr[i]);
        }

        if(N==1){
            Res = Arr[0];
        }
        else{
            while(sz(q) > 1){
                x = q.top(); q.pop();
                y = q.top(); q.pop();
                z = x+y;
                Res += z;
                q.push(z);
            }
        }
        printf("%lld",Res);
        if(T!=0)
            printf("\n");
    }
	return 0;
	*/
}

