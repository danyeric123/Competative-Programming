/*
-----------------------------------------------------------------------------
Author : - PVSM Praveen Kumar -
-----------------------------------------------------------------------------
*/

#include<bits/stdc++.h>
using namespace::std;

#define ll  long long
#define ull unsigned ll
#define LD long double

#define MP make_pair
#define BS binary_search
#define GCD __gcd
#define pb push_back
#define pp pop_back
#define F first
#define S second

#define amin(a,b) a=min(a,b)
#define amax(a,b) a=max(a,b)

#define sd(x) scanf("%d",&x)
#define pd(x) printf("%d",x)
#define sll(x) scanf("%lld",&x)
#define pll(x) printf("%lld",x)

#define gcj(i) printf("Case #%d: ",i);
#define ln printf("\n");
#define sp printf(" ");


#define Rep(i,n) for (int i=0; i<n; i++)
#define Fox(i,a,b) for (int i=a; i<=b; i++)
#define RFox(i,b,a) for (int i=b; i>=a; i--)


#if defined(_MSC_VER) || __cplusplus > 199711L
	#define itr(it,x) auto it=(x)
#else
	#define itr(it,x) __typeof(x) it = (x)
#endif

#define Each(it,A) for(itr(it,(A).begin()); it != (A).end(); ++ it)

#define amin(a,b) a=min(a,b)
#define amax(a,b) a=max(a,b)
#define sz(s) int((s).size()